{% extends '::base.html.twig' %}



{% block body -%}
    <h1>ProjectType list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Name</th>
                <th>Id</th>
                <th>Slug</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('projecttype_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td>{{ entity.id }}</td>
                <td>{{ entity.slug }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('projecttype_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('projecttype_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('projecttype_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
